# WirForce 2026 BYOC 座位查詢

讓 WirForce 2026 BYOC 玩家用暱稱或座位編號查到座位、同桌、對面、隔壁與背後的人。
活動期間：2026-09-24 ～ 09-27（台北世貿一館）。活動期間的修正以「快速上線、不破壞查詢」為最高優先。

## 範圍

- 主體是 BYOC 座位區。**2026-09-21 起 Yellow 追加**：空白領域介紹、留言板、美食與設施三頁，
  所以場地上的吃喝與設施標籤（星巴克、7-11、郵局、廁所、出入口…）現在納入。
  官方攤位與舞台仍然留在原表，不做。
- 介面文字一律使用繁體中文，專有名詞才用英文。
- 使用情境是現場手機、網路壅塞：頁面保持單一靜態檔、輕量，並保留 localStorage 離線快取。

## 資料來源

社群維護的 Google Sheets 座位表（公開檢視），Sheet ID：
`13n2z8QgxQL-xWLR2K4zy75Xw1aeu6Grf-DuGvZBckXY`

這是一張「地圖式」表格，不是資料表；維護者隨時會改版面。一律下載 **xlsx**（`/export?format=xlsx`）解析，因為只有 xlsx 保留合併儲存格，大桌判斷靠它。CSV 與 gviz 端點都會遺失合併資訊。

## 版面慣例（解析規則的依據）

- **工作表**：`世貿場地` 是完整的場地地圖，也是維護者實際在更新的那張。`快速座位表` 是同版面的精簡複本但常常比較舊，`美食座位地圖` 只是一份空白編號清冊。程式不寫死名稱，而是每張都解析一次，挑「座位最多、其次登記人數最多」的那張。
- **座位編號格**：原表寫成 `A01`、`B123`、`C466`，解析後一律正規化成 **1 英文 + 4 數字、不足補 0**（`A0001`、`B0123`、`C0466`）。同一列至少 3 個才算「座位編號列」，藉此排除說明文字裡零星出現的編號（例如表頭的「傳送門：A區A01」）。
- **空白領域**：14 個營區（`空白領域01 夢想領域ACG` 這種），沒有座位編號，也不佔座位格線。單獨放在 `zones`，不混進 `seats`，也不會被當成暱稱。
- **區塊**：試算表的同一列會同時橫跨 A/B/C 三區，三區的排法與方向各自獨立，所以要先把每一列切成「欄位相鄰、同一區」的區塊，再逐塊判斷方向。整列一起判斷會把 B 區的暱稱當成 A 區的。
- **方向**：座位編號是蛇行的，所以一個區塊由小到大就是面上、由大到小就是面下；走道欄位的「面上／面下」標記與暱稱實際寫在哪一側是兩個交叉驗證的線索，三者不一致才需要人工確認。
  - 面上排：編號在上、暱稱寫在下一列
  - 面下排：暱稱寫在上一列、編號在下
- **大桌**：暱稱儲存格合併跨兩三個座位 = 一人佔多個位置。合併超過 3 個座位的是走道說明（例如「酒精狂徒社交走道」），不是暱稱。
- **同桌對面**：往暱稱那一側找第一個有座位的列，兩邊要面向相反、編號也要接得上（中間只能跳過原表沒有的號碼）。原表會在同一張桌子中間插入社交走道說明，所以兩排可能隔到 8 列。
- **背後**：兩排編號列上下緊貼、面向相反 = 背對背。
- 各區每排座位數：A 區 14、B 區 14、C 區 18，但會因障礙物有誤差，所以座位位置一律以儲存格內容比對，不用座標推算。
- 座位格底色沿用原表顏色，頁面小地圖照原表配色呈現。

## 驗證解析結果

每次改動 `build_seats.py` 或原表改版後，用真實資料跑一次：

```bash
python build_seats.py --sheet-id 13n2z8QgxQL-xWLR2K4zy75Xw1aeu6Grf-DuGvZBckXY
```

完成條件：
1. 工作表要是 `世貿場地`，座位數 A 244／B 306／C 461、空白領域 14 個（2026-09-20 實測）。
   注意 A0252、B0306、C0466 是**編號上限**不是座位數：地圖上 A0161～A0168、C0325～C0328、C0332 本來就不存在（障礙物）。
   程式會印出每區的缺號，差距要能被這份缺號清單解釋。
2. 除了下面「已知的原表問題」那兩個以外沒有 ⚠ 警告；有新的警告就對照原表釐清，而不是把警告靜音。
3. 每區抽查至少 3 個座位（含一個大桌、一個面上排、一個面下排），確認暱稱、對面、背後都與原表一致。
4. 本機 `python -m http.server` 開頁面（**不能用檔案總管直接點開 `index.html`**，`fetch` 讀不到
   `seats.json`，會退回只有 A 區的範例資料），查詢一個暱稱與一個座位編號都能正確顯示，
   地圖三區加空白領域都在。
   要驗 `/zones`、`/board`、`/food`、`/seat/A0024` 這些網址得改跑 `SHEET_ID=… python server.py`，
   `http.server` 不會把它們導回 `index.html`，只會回 404。

## 部署

**現況：Zeabur + `server.py`（2026-09-21 起，Yellow 指定）。**
`server.py` 是一個 stdlib-only 的小服務：背景每 `REFRESH_MINUTES`（預設 30）分鐘重抓座位表，
解析結果只放在記憶體，同時送出 `index.html` 與 `seats.json`。`Dockerfile` 給 Zeabur 用。
環境變數：`SHEET_ID`（必填）、`REFRESH_MINUTES`、`DATA_DIR`（預設 `/data`）、`PORT`（Zeabur 自帶）。

設計上的三個重點，改動前先想清楚：
- **抓失敗要沿用上一份成功的資料**，只有從頭到尾沒抓成功過才回 503。現場網路不穩時這是救命的。
- 失敗後每分鐘重試，成功才回到 30 分鐘，不要讓一次失敗等滿半小時。
- **座位資料不寫磁碟。** 容器檔案系統可能唯讀，而且它本來就不需要落地 —— 掉了重抓就有。
  `DATA_DIR`（Zeabur 掛上來的 Volume）是留給**沒有其他來源的資料**用的，例如之後的留言板。
  兩者不要混：座位表的真相在 Google Sheets，硬碟上的東西掉了沒地方補。

`seats.json` 會 gzip 後送出（254 KB → 28 KB），並帶 ETag；頁面用 `cache:"no-cache"` 取，
所以沒變動時是 304 空回應。現場網路壅塞，這兩件事不要拿掉。

GitHub Pages 那套（`.github/workflows/update-seats.yml`）**目前停用**但留著當備案，
要切回去就 `gh workflow enable "更新座位資料"` 並把 Pages source 設成 GitHub Actions。
注意 Pages 只會送 `/`：`/zones`、`/board`、`/food`、`/seat/A0024` 這些網址在 Pages 上會 404
（分享出去的座位連結會失效），真的要切回去得補一份 `404.html` 之類的轉址。
repo 已設好 Actions variable `SHEET_ID`。

再換平台或改架構前，先與 Yellow 確認。

## 單一座位頁（`/seat/A0024`）

**2026-09-22 起 Yellow 追加。** 每個座位編號有自己的網址，可以單獨分享給人。
內容就是原本的查詢結果卡片，加上「介紹與活動」一段。

- **介紹與活動的來源就是留言板那一份**（`find_notes()` 抓的備註欄），只是改用座位編號索引，
  同一則備註會同時出現在 `/board` 和那個座位的頁面。**沒有另外新增資料來源，也沒有寫入功能。**
  大桌的兩個編號是同一個人，任一邊的備註兩頁都顯示，並標註「寫在同桌的 C0093」。
- 後端 `SEAT_PATH_RE` 只放行「1 英文 + 1～4 數字」，其餘 404；
  前端 `seatOfPath()` 把 `/seat/a24` 正規化成 `/seat/A0024`（`replaceState`，不多留一筆歷史）。
  原表沒有的編號（例如 `A0161`）會顯示「找不到這個座位」而不是空白頁。
- **`index.html` 裡讀 `seats.json` 一定要用 `/seats.json`**：在 `/seat/A0024` 這種多一層的路徑上，
  相對路徑會變成 `/seat/seats.json`，頁面會靜悄悄地退回 localStorage 的舊資料。
- 曝光程度（**2026-09-22 Yellow 改過一次，以這版為準**）：標題與社群預覽放
  **這個座位自己的**暱稱（`A0024　某某`，沒人登記就退回 `A0024　A 區第 2 排`），
  因為分享的就是自己的位置。**但別人的暱稱一個都不放**——對面、隔壁、背後是誰要點進來才看得到，
  不會有人貼一個連結就順便把鄰居的暱稱攤到聊天室裡。也沒有做 sitemap 或任何全名單索引。
  要再放寬，先與 Yellow 確認，見下方協作原則。
- **社群預覽由後端寫死（2026-09-22 起）。** 貼連結到 Discord／LINE／Threads 時，對方的伺服器
  只抓 HTML、不執行 JavaScript，所以前端 `route()` 設的標題它們看不到。`server.py` 的
  `seat_meta()` 針對 `/seat/XXXX` 換掉 `index.html` 裡 `<!--meta-->…<!--/meta-->` 那一段。
  **改 `index.html` 的 `<head>` 時不要動掉這兩個標記**，`main()` 找不到會直接拒絕啟動。
  `seat_meta()` 只會用到**傳進去的那一個座位**，不查 `by_id`，所以結構上碰不到鄰居的暱稱。
  改它的時候不要為了「順便顯示對面是誰」去查別的座位，那會直接違反上面那條決定。
  前端 `shareTitle()` 是同一套規則（分頁標題、分享文字都走它），兩邊要一起改。

## 單一營區頁（`/zone/01`）

**2026-09-22 起 Yellow 追加。** 跟座位頁同一套：前端 `zoneOfPath()` 正規化（`/zone/1` → `/zone/01`），
後端 `ZONE_PATH_RE` 放行「1～2 位數字」，原表沒有的號碼送原版頁面讓前端顯示「找不到」。

- 介紹來源就是 `/zones` 那一頁的 `about`，**沒有新增資料來源**。
- 社群預覽（`zone_meta()`）**放介紹全文**（截 150 字）。這跟座位頁不放鄰居暱稱不衝突：
  營區介紹是那一團自己寫在公開表上、就是要給人看的自我介紹，不是別人的個資。
- **地圖上點營區不走「先放大再點第二下」那條路**。那個防誤觸是給座位用的（全區時一個座位
  才 3～4 px）；營區方塊大得多又有編號，點它就是刻意的。要求點第二下的實際結果是
  放大後畫面重新置中、第二下打到旁邊的座位，使用者會以為點了沒反應。
- 卡片在地圖**下方**，所以 `$out.scrollIntoView` 用 `block:"start"`。用 `nearest` 遇到
  比畫面高的卡片只會露出底部，同樣像沒反應。

## 其他三個頁面的資料來源

- **空白領域介紹**：寫在「空白領域」工作表裡、團名那格的**正下方**。14 個營區目前只有 1 則
  （空白領域06 →「深夜食堂 及日版KTV」），頁面會照實顯示「還沒有填介紹」，原表補了就會自動出現。
- **留言板**：`find_notes()` 掃出有「編號／ID／備註」三欄的工作表（目前叫「美食座位地圖」，
  **名字會騙人，它不是美食地圖**），取備註欄。目前 5 則。同一份資料也餵給座位頁的
  「介紹與活動」（2026-09-22 實測 7 則有填座位編號）。
- **美食與設施**：`find_places()` 靠關鍵字從場地圖挑招牌。原表沒有任何欄位標記這些，所以：
  - 維護者的閒聊也常提到星巴克、7-11，用 `CHATTY_RE`（有句讀就不是招牌）排掉
  - 座位帶範圍內的一律不算，那是暱稱
  - 抓到的會列在 `places`，**改關鍵字後請人工看一遍輸出**，不要只看筆數

## 已知的原表問題（要找維護者，不是程式的 bug）

- `世貿場地` 的 **`世界支柱` 貼了兩次**（ED15 與 ED71）。ED15 對得到 C0037～C0039，ED71 落在沒有座位的空白處，所以會固定跳一個警告。
- **空白領域11** 在 `世貿場地` 寫「DJ-音樂放縱-重聽部」，但 `快速座位表` 與 `空白領域` 兩張都寫「勇者不睡覺」，程式會警告但以所選工作表為準。
- `快速座位表`（gid 2015154201）版面比較乾淨，但**暱稱比 `世貿場地` 少** —— 2026-09-20 實測少 21 個、多 6 個、9 個兩邊不一致。等維護者補齊再考慮換過去。

## 全區地圖

`index.html` 的「看全區地圖」是**相對位置圖**：格線直接沿用原表的列／欄（`seats.json` 的 `cell` 就能還原座標），所以障礙物缺口與走道會照原表留白，但**不是按實際場地比例畫的、也沒有方位**。世貿地圖與實際場地的對應由 Yellow 另外處理，這裡不要自己加地標或方位。

目前是 **A 版**：圖上只顯示「有人／空位」與原表底色，暱稱要點下座位才出現，曝光程度跟單筆查詢一樣。空白領域以虛線色塊畫在座位格線的上下方與左側（原表就畫在那裡），可以用團名搜尋。

整張圖有 59 欄，「大」的時候寬約 2600px 但手機只看得到 8 欄，**一定要有辦法導航**，否則使用者會以為只有 A 區：
- 工具列有 `A 區／B 區／C 區／空白領域` 快速跳轉（全區時會先自動放大再跳）
- 換縮放時保留畫面中心的相對位置，不會被丟回最左上角（最左邊是空白領域，不是 A 區）
- 區域標題 `position:sticky` 固定在上緣，捲動時知道自己在哪一區

要讓暱稱直接畫在圖上（B 版）等同於在頁面上攤開全名單，**先與 Yellow 確認**，見下方協作原則。

座位座標完全由前端從 `cell` 算出來（`CW9` → 第 9 列第 101 欄），`seats.json` 不另外存 r/c。
載入時會把舊格式的編號（`A01`）補成 4 碼，所以使用者手機裡的舊 localStorage 快取也能直接用。

## 協作原則

- 座位表是社群維護者的心血，頁面 footer 保留原表連結與「以原表為準」的說明。
- 暱稱屬於玩家自行登記的公開資訊；新增任何會擴大曝光的功能（例如全名單列表、外部索引）前先與 Yellow 確認。
- push 到 main 會觸發正式部署，commit 前先完成上面的驗證。
